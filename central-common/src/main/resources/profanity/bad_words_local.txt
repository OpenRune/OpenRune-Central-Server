# One blocked phrase per line (case-insensitive substring match on canonical account name).
# Lines starting with # are ignored.
# Merged with profanity/bad_words_custom.txt, then the remote gist list configured on Central.
